GEMINI_API_KEY=your_gemini_api_key
# Define this to send profile and daily log writes through the FastAPI backend.
VITE_API_URL=http://localhost:8000
# Android/production builds must use the public HTTPS API endpoint instead:
# VITE_API_URL=https://api.reconexaoessencial.com.br
# Local-only backend test mode; never set this in a deployed frontend.
# VITE_API_DEV_TOKEN=dev-token
